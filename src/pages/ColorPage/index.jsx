

const Color = () => {
    return (
        <h1>This will be a color page</h1>
        // useParams and function that maps through global state and checks to find the name of right color then adds to state 
        // List the right color here using the global state colors passed in
    )
}

export default Color;
